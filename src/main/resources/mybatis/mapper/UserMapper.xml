<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.easystaff.mapper.UserMapper">

    <resultMap id="UserResultMap" type="com.example.easystaff.domain.User">
        <id column="id" property="id"/>
        <result column="username" property="username"/>
        <result column="password" property="password"/>
        <result column="nickname" property="nickname"/>
        <result column="role" property="role"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <select id="findByUsername" parameterType="string" resultMap="UserResultMap">
        SELECT id, username, password, nickname, role, create_time, update_time
        FROM user
        WHERE username = #{username}
    </select>

    <insert id="insert" parameterType="com.example.easystaff.domain.User" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO user (username, password, nickname, role, create_time, update_time)
        VALUES (#{username}, #{password}, #{nickname}, #{role}, NOW(), NOW())
    </insert>

    <sql id="UserWhere">
        <where>
            <if test="username != null and username != ''">
                AND username LIKE CONCAT('%', #{username}, '%')
            </if>
            <if test="role != null and role != ''">
                AND role = #{role}
            </if>
        </where>
    </sql>

    <select id="findPage" resultMap="UserResultMap">
        SELECT id, username, password, nickname, role, create_time, update_time
        FROM user
        <include refid="UserWhere"/>
        ORDER BY id ASC
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="count" resultType="long">
        SELECT COUNT(*)
        FROM user
        <include refid="UserWhere"/>
    </select>

    <update id="updatePassword">
        UPDATE user SET password = #{password}, update_time = NOW() WHERE id = #{id}
    </update>

    <update id="updateRole">
        UPDATE user SET role = #{role}, update_time = NOW() WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM user WHERE id = #{id}
    </delete>

</mapper>


